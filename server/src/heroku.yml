setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
    - plan: heroku-redis:hobby-dev
build:
  docker:
    web: Dockerfile
release:
  command:
    - ./scripts/release.sh
  image: web
run:
  web: node dist/src/main
