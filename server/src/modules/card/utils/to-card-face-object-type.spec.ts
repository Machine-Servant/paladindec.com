import { ToCardFaceObjectType } from './to-card-face-object-type';

describe('ToCardFaceObjectType', () => {
  it('should be defined', () => {
    expect(new ToCardFaceObjectType()).toBeDefined();
  });
});
