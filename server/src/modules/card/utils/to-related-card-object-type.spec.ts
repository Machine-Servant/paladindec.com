import { ToRelatedCardObjectType } from './to-related-card-object-type';

describe('ToRelatedCardObjectType', () => {
  it('should be defined', () => {
    expect(new ToRelatedCardObjectType()).toBeDefined();
  });
});
