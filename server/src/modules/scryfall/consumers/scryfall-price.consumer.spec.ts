import { ScryfallPriceConsumer } from './scryfall-price.consumer';

describe('ScryfallPriceConsumer', () => {
  it('should be defined', () => {
    expect(new ScryfallPriceConsumer()).toBeDefined();
  });
});
