import { BulkDownloadConsumer } from './bulk-download.consumer';

describe('BulkDownloadConsumer', () => {
  it('should be defined', () => {
    expect(new BulkDownloadConsumer()).toBeDefined();
  });
});
